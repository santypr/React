

export class PeopleApi {
    // async getAllAsync(): Promise<People[]> {
    //     const token = await getAuthToken();
    //     return this.setupRequest('GET', `${baseUrl}/${this.getTenantId()}`)
    //       .withOAuth({ token: `${token}` })
    //       .execute<Survey[]>();
    //   }
    
    
}